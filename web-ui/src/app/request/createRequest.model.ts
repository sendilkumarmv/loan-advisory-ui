export interface CreateCallBackRequest {
    firstName: string;
    lastName: string;
    mobileNumber: string;
}

export interface CreateCallBackResponse {
    firstName: string;
    lastName: string;
    mobileNumber: string;
    createdTime: string;
}